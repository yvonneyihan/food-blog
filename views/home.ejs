<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>My Food Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Winky+Rough:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
    <style>
      .card-text {
        max-height: 4.5em;
        overflow: hidden;
        text-overflow: ellipsis;
      }
    </style>
  </head>

  <body>

    <header class="py-3 mb-4 border-bottom bg-dark text-white">
      <div class="container d-flex flex-wrap justify-content-between align-items-center">
        <div class="d-flex flex-column">
            <a href="/" class="header-text d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none fs-2">
            🍜 My Food Blog
            </a>
            <p class="header-text lead text-white">Explore delicious dishes, cooking tips, and personal food stories!</p>
        </div>
        <div class="d-flex justify-content-end align-items-center gap-2">
            <% if (session?.userId) { %>
                <a href="/posts/compose" class="btn btn-outline-light">New Post</a>
                <a href="/users/dashboard" class="btn btn-outline-light">Dashboard</a>

                <div class="dropdown">
                <a class="btn btn-light me-2 dropdown-toggle <%= isCategory ? 'active bg-white' : 'bg-dark text-white' %>" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Category
                </a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/posts/category/Restaurant Review">Restaurant Review</a></li>
                    <li><a class="dropdown-item" href="/posts/category/Recipe">Recipe</a></li>
                    <li><a class="dropdown-item" href="/posts/category/Cooking Tip">Cooking Tip</a></li>
                </ul>
                </div>

                <a href="/users/logout" class="btn btn-danger">Log Out</a>
            </div>
          <% } else { %>
            <a href="/users/login" class="btn btn-outline-light me-2">Login</a>
            <a href="/users/register" class="btn btn-outline-light">Register</a>
          <% } %>
        </div>
      </div>
    </header>

    <main>
      <section class="py-5 text-center container">
        <div class="row py-lg-5">
          <div class="col-lg-6 col-md-8 mx-auto">
            <% if (session?.userId) { %>
                <h1 class="user-name fw-light">Hi, <%= session.username %>!</h1>
            <% } else { %>
                <h1 class="header-text fw-light">Welcome to My Food Blog</h1>
            <% } %>
          </div>
        </div>
      </section>

      <div class="album py-5 bg-light">
        <div class="container">
          <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            <% if (posts.length > 0) { %>
                <% posts.forEach(function(post) { %>
                    <div class="col">
                        <div class="card shadow-sm h-100">
                        <div class="card-body d-flex flex-column">
                            <% if (post.image_url) { %>
                            <img src="<%= post.image_url %>" class="card-img-top" alt="Post image">
                            <% } %>
                            <h5 class="card-title mb-2" style="min-height: 3em;"><%= post.title.length > 80 ? post.title.substring(0, 80) + '...' : post.title %></h5>
                            <div class="d-flex justify-content-between text-muted small mb-3">
                                <span>#<%= post.category %></span>
                                <span><%= new Date(post.updated_at).toLocaleDateString() %></span>
                            </div>            
                            <p class="card-text mb-3" style="min-height: 80px;"><%= post.content.length > 100 ? post.content.substring(0, 100) + '...' : post.content %></p>
                            <div class="d-flex gap-2 mt-auto pt-3">
                                <a href="/posts/read/<%= post.id %>" class="btn btn-sm btn-outline-primary">Read</a>
                                <!-- <a href="/posts/edit/<%= post.id %>" class="btn btn-sm btn-outline-secondary">Edit</a> -->
                            </div>
                        </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No posts found.</p>
            <% } %>
          </div>
        </div>
      </div>
    </main>

    <footer class="text-body-secondary py-5 bg-white">
      <div class="container text-center">
        <p class="mb-0">© <%= new Date().getFullYear() %> My Blog</p>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>


