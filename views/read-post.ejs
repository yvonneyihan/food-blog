<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= post.title %> | My Food Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/styles.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Ga+Maamli&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap" rel="stylesheet" />
  <style>
    .post-content {
      white-space: pre-wrap;
    }
  </style>
</head>

<%- include("header.ejs") %>

<body>
  <div class="container mt-5">
    <div class="card shadow-sm p-4 mb-4">
        <% if (post.image_url) { %>
        <img src="<%= post.image_url %>" class="card-img-top mx-auto d-block mb-4"
        alt="Post image" style="max-width: 300px; height: auto;">
        <% } %>
        <h1 class="mb-3"><%= post.title %></h1>
        <span class="badge bg-secondary mb-3">#<%= post.category %></span>
        <p class="lead post-content"><%= post.content %></p>
        <% if (session?.userId) { %>
        <a href="/users/dashboard" class="btn btn-outline-primary mt-4">← Return to Dashboard</a>
        <% } else { %>
        <a href="/posts" class="btn btn-outline-primary mt-4">← Back to Home</a>
        <% } %>
    </div>
  </div>
</body>
</html>
